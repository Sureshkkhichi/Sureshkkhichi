# User Preferences & Workflow Rules

- **Git Automation**: Always automatically run git commands (`git add`, `git commit`, and `git push`) after completing any task.
- Use clear, professional commit messages following conventional commits (e.g. `feat: ...`, `fix: ...`, `docs: ...`).
- If push fails due to credentials or network, inform the user with the exact cause and command.
